"use client";

// THROWAWAY preview route for screenshotting CreatePlanModal's Host picker.
// Deleted before commit — do not ship.

import CreatePlanModal from "@/components/CreatePlanModal";

export default function SeriesPreviewPage() {
  return (
    <CreatePlanModal
      calendarId="HFxXUedAMK"
      tier="starter"
      onClose={() => {}}
      onCreated={() => {}}
    />
  );
}
