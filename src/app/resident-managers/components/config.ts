// CTAs for the /resident-managers landing.
// DEMO_URL = the Concierge booking link (same Google Calendar slot
// used by the /organizations marketing page so all "Book a demo"
// CTAs across the site converge on one calendar).
// FREE_URL = the free-tier signup at /apartment (relative path so it
// stays within os.joinleaf.com without a hard-coded host).
export const DEMO_URL = "https://calendar.app.google/4v3mg4Ry4i5Bn7fG8";
export const FREE_URL = "/apartment";
